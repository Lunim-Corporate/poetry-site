# Copy this file to `.env` or `.env.local` and fill in your values.
# Next.js loads `.env.local` first (and overrides `.env`).

# -----------------------------------------------------------------------------
# Prismic CMS
# -----------------------------------------------------------------------------
# Optional: overrides repository name from slicemachine.config.json (default: poetry-site)
NEXT_PUBLIC_PRISMIC_ENVIRONMENT=

# -----------------------------------------------------------------------------
# Stripe (payments — see PaymentForm, api/checkout, enter/success)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=

# -----------------------------------------------------------------------------
# Google Sheets (service account — private key often uses \n for newlines in .env)
# -----------------------------------------------------------------------------
GOOGLE_SHEETS_SPREADSHEET_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=

# -----------------------------------------------------------------------------
# Mailchimp (newsletter API route)
# -----------------------------------------------------------------------------
MAILCHIMP_API_KEY=
MAILCHIMP_AUDIENCE_ID=

# -----------------------------------------------------------------------------
# Resend + transactional email
# -----------------------------------------------------------------------------
RESEND_API_KEY=
EMAIL_FROM=
ADMIN_EMAIL=
